# The lecture vocabulary

Generated by `npm run docs:vocabulary -w schema` from `studio/schema/src/schema.js`
and the committed lectures under `courses/waves-and-vibrations/`. **Do not edit**,
except between the two `hand-written` markers — that part is copied through on
every regeneration, and `--check` fails the build when the rest is stale.

This is the list of things a lecture is allowed to be made of. It is also the
sanitiser: whatever the schema does not know cannot reach a student's browser as
live markup, so a `PUT` carrying anything else comes back normalised without it.
Every count below is what the four committed lectures actually contain, which is
what makes an unfamiliar class suspicious rather than merely unusual.

Documents surveyed: `L01-free-oscillations/notes`, `L01-free-oscillations/slides`, `L02-damped-driven/notes`, `L03-coupled-oscillators/notes`, `L04-chain-of-oscillators/notes`, `L04-chain-of-oscillators/slides`.

<!-- hand-written: kept when this file is regenerated -->

## What the classes mean

The vocabulary is small because the *node* list is closed and the meaning lives
in the class. These are the classes the course actually uses, and what each one
claims:

| Class | On | Means |
|---|---|---|
| `result` | `div` | A boxed result — the thing that stays on screen while the board moves on. The one place a formula is allowed to be the whole content of a block. `result wide` when it needs the full measure. |
| `board-note` | `div` | **What gets derived on the board.** It says exactly which step happens in chalk, so the deck and the board cannot drift apart. A slide stating a result whose derivation is on the board carries one. |
| `deriv` | `div` | A derivation in the notes: the lines a slide is not allowed to have. |
| `think` | `div` | A question to the room, usually holding a `details`/`summary` pair whose summary is the word תשובה. |
| `try` | `div` | Something for the reader to do with a demo. |
| `note` | `div`, `p` | An aside. Quieter than the surrounding prose, not a different kind of claim. |
| `cols`, `cols top` | `div` | Two columns. Layout, not meaning. |
| `summary` | `div` | The end-of-section recap. |
| `fig`, `fig-slide` | `figure` | A figure in the notes; the `-slide` variants are cut for the deck's measure. |
| `demo-block` | `figure` | A figure whose content is an interactive demo — the `div` inside it is the mount, and `lecture.json` says what mounts there. |
| `pending` | `figure` | **A request, not content.** `fig pending` or `demo-block pending` with a `p.brief` inside saying what it should show. The theme labels it: "in preparation" to a student, "within 24 hours" inside the studio. This is how a lecturer asks for a figure she cannot make herself. |
| `lab` | `span` | The label of a boxed result — "quality factor", "dispersion relation". It is a mark on the leading run, not a child node. |
| `en` | `span` | The English technical term, in parentheses, on first use in a lecture. `glossary.md` is the authority on which Hebrew term it follows. |
| `ltr` | `span`, `td` | A left-to-right island inside Hebrew: a symbol, a number, a file name. |
| `cap`, `hint`, `muted`, `tag` | `span` | Caption text, a hint, quieter text, a small label. |
| `ans` | `p` | The answer inside a `details`. |
| `sub`, `lead`, `small`, `thesis` | `p` | Type roles: a standfirst, a lead paragraph, small print, the claim a section argues. |
| `fragment` | any | Reveal's step-reveal on a slide. Meaningless in the notes. |

## Three rules

1. **Hebrew never goes inside math.** KaTeX has no Hebrew metrics and renders
   boxes. Put the Hebrew in HTML *around* the formula — `מ־$\omega_0$`, never
   `$מ-\omega_0$`. The tokeniser only ever splits a text node at the
   delimiters, so it cannot save you from this; it can only preserve what you
   wrote.
2. **`.board-note` marks what is derived on the board.** The deck exists to
   carry what the board cannot. If a slide states a result and the derivation
   happens in chalk, say so in a `.board-note`, in one sentence, naming the
   step.
3. **A slide that restates a paragraph of the notes should not exist.** The
   notes are written first and carry every derivation; the deck is *cut from*
   them and keeps only the boxed results, the setup figures and the demos.

<!-- end hand-written -->

## Groups

What may go inside what. A content expression above names one of these.

| Group | Members |
|---|---|
| `(structure)` | `listItem`, `figcaption`, `summary`, `tableRow`, `tableCell`, `tableHeader` |
| `block` | `lectureHead`, `section`, `slide`, `heading`, `paragraph`, `looseText`, `callout`, `blockquote`, `list`, `figure`, `image`, `demoMount`, `details`, `table`, `speakerNotes`, `horizontalRule`, `mathBlock`, `raw` |
| `inline` | `text`, `mathInline`, `hardBreak`, `rawInline` |

## Blocks

### `lectureHead` — `<header>`

- **group** — `block`
- **content** — `block+`
- **attributes** — `id`
- **gets a `data-id`** — yes
- **in the lectures** — 4
- **classes seen** — `notes-head` ×4

```html
<header class="notes-head">
  <h1>תנודות מרוסנות ומאולצות</h1>
  <p class="sub">בהרצאה הקודמת המתנד התנודד לנצח. מתנד אמיתי מאבד אנרגיה, ונוסיף זאת למודל. הריסון מכניס למשוואה <em>סקאלת זמן שנייה</em>, והיחס בין שתי הסקאלות הוא גורם האיכות $Q$, שיופיע בהרצאה שלוש פעמים בהקשרים שונים. לאחר מכן נפעיל על המתנד כוח חיצוני מחזורי ונגיע ל<strong>תהודה</strong>.</p>
</header>
```

### `section` — `<section>`

- **group** — `block`
- **content** — `block+`
- **attributes** — `id`, `anchor`, `num`, `title`
- **gets a `data-id`** — yes
- **in the lectures** — 76
- **classes seen** — _none in the committed lectures_

```html
<section id="s19">
  <h2><span class="n">19</span><span class="t">קריאה</span></h2>
  <ul>
    <li>
      <strong>Georgi</strong>, <em>The Physics of Waves</em> — ch. 3, ch. 4.
    </li>
    <li>
      <strong>Berkeley Physics Course</strong> vol. 3 (Crawford), <em>Waves</em> — pp. 16, 23–24, 28–29, 32–35.
    </li>
  </ul>
  <p class="note">התרגילים נמצאים בגיליון נפרד.</p>
</section>
```

### `slide` — `<section>`

- **group** — `block`
- **content** — `block+`
- **attributes** — `id`, `cls`, `anchor`
- **gets a `data-id`** — yes
- **in the lectures** — 41
- **classes seen** — `title` ×2

```html
<section>
  <h2 class="demo-title">מסה על קפיץ, חי</h2>
  <div id="demo-mass-spring"></div>
  <aside class="notes">
    1. לשחרר ממנוחה — ההיטל של החץ הוא מיקום הגוש. 2. להכפיל את המשרעת — זמן המחזור לא משתנה (לזכור לשקף 7). 3. לגרור את חץ המהירות — המשרעת גדלה, המיקום ההתחלתי לא.
  </aside>
</section>
```

### `heading` — `<h1 … h6>`

- **group** — `block`
- **content** — `inline*`
- **attributes** — `id`, `level`, `cls`, `style`
- **gets a `data-id`** — yes
- **in the lectures** — 62
- **classes seen** — `demo-title` ×4, `en-sub` ×2

```html
<h3 class="en-sub">free oscillations</h3>
```

### `paragraph` — `<p>`

- **group** — `block`
- **content** — `inline*`
- **attributes** — `id`, `cls`, `style`
- **gets a `data-id`** — yes
- **in the lectures** — 456
- **classes seen** — `ans` ×15, `small muted` ×15, `demo-link no-print` ×11, `small` ×11, `lead` ×10, `lead fragment` ×9, `note` ×4, `sub` ×4, `byline` ×2, `kicker` ×2, `examples` ×1, `freq-strip` ×1, `small fragment` ×1, `small muted fragment` ×1, `stamp` ×1, `thesis` ×1

```html
<p>נאסוף את $N$ ההסטות לוקטור אחד, $\vec\psi = (\psi_1,\dots,\psi_N)^T$, ונכתוב את $N$ המשוואות כמשוואה אחת, בדיוק כמו בסעיף 5 של הרצאה 3.</p>
```

### `looseText` — no element of its own

- **group** — `block`
- **content** — `inline*`
- **attributes** — _none_
- **gets a `data-id`** — no
- **in the lectures** — 338
- **classes seen** — _none in the committed lectures_

```html
<b>סרקו את $\omega$ מלמטה כלפי מעלה.</b> בתדירות נמוכה המסה נעה יחד עם הכפייה; בגבוהה היא נעה הפוך ממנה וכמעט אינה זזה.
```

### `callout` — `<div>`

- **group** — `block`
- **content** — `block+`
- **attributes** — `id`, `cls`, `style`
- **gets a `data-id`** — yes
- **in the lectures** — 289
- **classes seen** — `result` ×109, `board-note` ×39, `deriv` ×36, `note` ×23, `result wide` ×15, `cols` ×14, `think` ×13, `try` ×10, `cols top` ×2, `summary` ×2

```html
<div class="result wide">
  <span class="lab">התוצאה הכללית</span>
  $$\psi(t) = |X|\;\underbrace{e^{(\Real s)\,t}}_{\text{envelope}}\;\underbrace{\cos\!\left((\Imag s)\,t + \varphi\right)}_{\text{oscillation}}$$
</div>
```

### `blockquote` — `<blockquote>`

- **group** — `block`
- **content** — `block+`
- **attributes** — `id`, `cls`
- **gets a `data-id`** — yes
- **in the lectures** — 0
- **classes seen** — _none in the committed lectures_

### `list` — `<ol|ul>`

- **group** — `block`
- **content** — `listItem+`
- **attributes** — `id`, `ordered`, `cls`, `style`, `start`
- **gets a `data-id`** — yes
- **in the lectures** — 32
- **classes seen** — `big-list` ×3, `conditions` ×1, `cycle` ×1, `fragment big-list` ×1

```html
<ul>
  <li>
    יהיו <strong>בדיוק $N$ אופנים</strong>, כמספר דרגות החופש — זו התוצאה הכללית של הרצאה 3.
  </li>
  <li>
    התדירות <strong>עולה עם מספר הצמתים</strong>. ככל שהצורה מתחלפת מהר יותר במרחב, ההפרשים $\psi_{n+1}-\psi_n$ גדולים יותר עבור אותה משרעת, ולפי סעיף 2 גם הכוח המחזיר.
  </li>
</ul>
```

### `figure` — `<figure>`

- **group** — `block`
- **content** — `(figcaption | block)+`
- **attributes** — `id`, `cls`, `anchor`
- **gets a `data-id`** — yes
- **in the lectures** — 46
- **classes seen** — `fig` ×21, `fig-slide` ×13, `demo-block` ×10, `fig-slide narrow` ×1, `fig-slide roadmap` ×1

```html
<figure class="fig">
  <img src="figures/reflection.svg" alt="שיקוף המטוטלות המצומדות סביב נקודת האמצע">
  <figcaption>שיקוף סביב הציר שבאמצע. המסות מחליפות תפקידים, וכל הסטה מתהפכת בסימנה משום ש־$x_1$ ו־$x_2$ נמדדות באותו כיוון.</figcaption>
</figure>
```

### `image` — `<img>`

- **group** — `block`
- **content** — _atom_
- **attributes** — `id`, `src`, `alt`, `cls`, `width`, `height`
- **gets a `data-id`** — yes
- **in the lectures** — 36
- **classes seen** — _none in the committed lectures_

```html
<img src="figures/guess-modes.svg" alt="צורות האופנים המנוחשות עבור N=1, 2, 3">
```

### `demoMount` — `<div>`

- **group** — `block`
- **content** — _atom_
- **attributes** — `id`, `mountId`, `cls`
- **gets a `data-id`** — yes
- **in the lectures** — 14
- **classes seen** — _none in the committed lectures_

```html
<div id="demo-chain-pluck"></div>
```

### `details` — `<details>`

- **group** — `block`
- **content** — `(summary | block)+`
- **attributes** — `id`, `cls`, `open`
- **gets a `data-id`** — yes
- **in the lectures** — 13
- **classes seen** — _none in the committed lectures_

```html
<details>
  <summary>תשובה</summary>
  <p class="ans" style="margin:.3em 0 0">לא. $A_{mod}$ מתאפסת שם, אך היא אינה נשארת אפס: היא ממשיכה לגדול בערכה המוחלט, כעת עם הסימן ההפוך. מסה 1 מתחילה מיד לקבל אנרגיה בחזרה ממסה 2, והמצב שבו "כל האנרגיה אצל מסה 2" נמשך רגע אחד בלבד.</p>
</details>
```

### `table` — `<table>`

- **group** — `block`
- **content** — `tableRow+`
- **attributes** — `id`, `cls`, `style`
- **gets a `data-id`** — yes
- **in the lectures** — 16
- **classes seen** — `dict` ×2, `defs` ×1, `dict three` ×1

```html
<table class="dict">
  <tr>
    <th>מסה על קפיץ</th>
    <th>מעגל LC</th>
    <th>התפקיד</th>
  </tr>
  <tr>
    <td>$m$</td>
    <td>$L$</td>
    <td>אינרציה</td>
  </tr>
  <tr>
    <td>$k$</td>
    <td>$1/C$</td>
    <td>כוח מחזיר</td>
  </tr>
  <tr>
    <td>$x$</td>
    <td>$Q$</td>
    <td>דרגת החופש</td>
  </tr>
</table>
```

### `speakerNotes` — `<aside>`

- **group** — `block`
- **content** — `block+`
- **attributes** — `id`, `cls`
- **gets a `data-id`** — yes
- **in the lectures** — 21
- **classes seen** — `notes` ×21

```html
<aside class="notes">
  זה בדיוק מה שעשינו בשקף הקודם: פרבולה במינימום. גם קפיצים אמיתיים אינם ליניאריים לאורך כל הטווח.
</aside>
```

### `horizontalRule` — `<hr>`

- **group** — `block`
- **content** — _atom_
- **attributes** — `id`
- **gets a `data-id`** — yes
- **in the lectures** — 0
- **classes seen** — _none in the committed lectures_

### `mathBlock` — no element of its own

- **group** — `block`
- **content** — _atom_
- **attributes** — `latex`, `delim`
- **gets a `data-id`** — no
- **in the lectures** — 89
- **classes seen** — _none in the committed lectures_

```html
$$\alpha\,\frac{\dd^2\psi}{\dd t^2} + \beta\,\frac{\dd\psi}{\dd t} + \gamma\,\psi = 0$$
```

### `raw` — no element of its own

- **group** — `block`
- **content** — _atom_
- **attributes** — `id`, `html`
- **gets a `data-id`** — yes
- **in the lectures** — 0
- **classes seen** — _none in the committed lectures_

## Inline

What may appear inside a textblock. `text` and the two math nodes are
the whole of it — everything else inline is a **mark**, below.

### `text` — no element of its own

- **group** — `inline`
- **content** — _atom_
- **attributes** — _none_
- **gets a `data-id`** — no
- **in the lectures** — 3015
- **classes seen** — _none in the committed lectures_

```html
, ממלאת את תפקיד האנרגיה הקינטית; האנרגיה האגורה בקבל, 
```

### `mathInline` — no element of its own

- **group** — `inline`
- **content** — _atom_
- **attributes** — `latex`, `delim`
- **gets a `data-id`** — no
- **in the lectures** — 1589
- **classes seen** — _none in the committed lectures_

```html
$\langle\cos^2\rangle = \langle\sin^2\rangle = \tfrac12$
```

### `hardBreak` — `<br>`

- **group** — `inline`
- **content** — _atom_
- **attributes** — _none_
- **gets a `data-id`** — no
- **in the lectures** — 4
- **classes seen** — _none in the committed lectures_

```html
<br>
```

### `rawInline` — no element of its own

- **group** — `inline`
- **content** — _atom_
- **attributes** — `html`
- **gets a `data-id`** — no
- **in the lectures** — 0
- **classes seen** — _none in the committed lectures_

## Structure

Nodes with no group: each is only ever the child of one particular node,
so it cannot be inserted on its own.

`listItem`, `summary`, `tableRow`, `tableCell`, `tableHeader` carry no `data-id` either — they are
addressed through their parent. `figcaption` does, because the editor can highlight one on its own.

### `listItem` — `<li>`

- **group** — _none — it is only ever a child of the node above it_
- **content** — `block+`
- **attributes** — `cls`, `style`
- **gets a `data-id`** — no
- **in the lectures** — 118
- **classes seen** — `fragment` ×4, `hl` ×1

```html
<li>
  כאשר $V''(x_0) = 0$: האיבר המוביל הוא $\psi^3$ או $\psi^4$, והתנועה אינה הרמונית גם במשרעות קטנות מאוד.
</li>
```

### `figcaption` — `<figcaption>`

- **group** — _none — it is only ever a child of the node above it_
- **content** — `inline*`
- **attributes** — `id`, `cls`
- **gets a `data-id`** — yes
- **in the lectures** — 46
- **classes seen** — _none in the committed lectures_

```html
<figcaption>האופן הסימטרי מוחזר על ידי $S$ עם סימן הפוך, והאנטי־סימטרי מוחזר לעצמו. כדי למצוא את שני האופנים די לשאול אם תמונת המראה זהה או הפוכה.</figcaption>
```

### `summary` — `<summary>`

- **group** — _none — it is only ever a child of the node above it_
- **content** — `inline*`
- **attributes** — `cls`
- **gets a `data-id`** — no
- **in the lectures** — 13
- **classes seen** — _none in the committed lectures_

```html
<summary>ההוכחה, בשתי שורות</summary>
```

### `tableRow` — `<tr>`

- **group** — _none — it is only ever a child of the node above it_
- **content** — `(tableCell | tableHeader)+`
- **attributes** — `cls`, `style`
- **gets a `data-id`** — no
- **in the lectures** — 84
- **classes seen** — _none in the committed lectures_

```html
<tr>
  <td>וקטור האופן</td>
  <td class="ltr">$\vec V^{(\ell)}$</td>
  <td>רכיביו $V^{(\ell)}_n$, לא מנורמלים</td>
</tr>
```

### `tableCell` — `<td>`

- **group** — _none — it is only ever a child of the node above it_
- **content** — `inline*`
- **attributes** — `cls`, `style`, `colspan`, `rowspan`
- **gets a `data-id`** — no
- **in the lectures** — 204
- **classes seen** — `ltr` ×89

```html
<td class="ltr">$X = a - ib = Ae^{i\varphi}$</td>
```

### `tableHeader` — `<th>`

- **group** — _none — it is only ever a child of the node above it_
- **content** — `inline*`
- **attributes** — `cls`, `style`, `colspan`, `rowspan`
- **gets a `data-id`** — no
- **in the lectures** — 40
- **classes seen** — `ltr` ×15

```html
<th class="ltr">$\lambda_\ell$</th>
```

## Marks

Marks nest in this order on the way out, outermost first:
`link` → `span` → `strong` → `b` → `em` → `i` → `code` → `sup` → `sub` → `u` → `small`.
`span` does not exclude itself — `<span class="lab">Q <span class="en">(quality
factor)</span></span>` is real markup in both lectures.

### `link` — `<a>`

- **attributes** — `href`, `cls`, `title`, `target`, `rel`, `demo`, `rep`, `show`
- **in the lectures** — 11
- **classes seen** — _none in the committed lectures_

```html
<a href="#demo-block-2" data-demo="#demo-coupled-pendula">חזרה להדגמה 2, ושבירת התנאי ← </a>
```

### `span` — `<span>`

- **attributes** — `cls`, `style`, `dir`
- **in the lectures** — 451
- **classes seen** — `lab` ×161, `en` ×84, `hint` ×83, `cap` ×64, `muted` ×14, `think-tag` ×4, `tag` ×1

```html
<span class="lab">משוואות התנועה</span>
```

### `strong` — `<strong>`

- **attributes** — _none_
- **in the lectures** — 144
- **classes seen** — _none in the committed lectures_

```html
<strong>העקומה אינה משתנה כלל</strong>
```

### `b` — `<b>`

- **attributes** — _none_
- **in the lectures** — 138
- **classes seen** — _none in the committed lectures_

```html
<b>לחצו על "10 Hz" והפעילו את הצליל.</b>
```

### `em` — `<em>`

- **attributes** — _none_
- **in the lectures** — 33
- **classes seen** — _none in the committed lectures_

```html
<em>הרציף</em>
```

### `i` — `<i>`

- **attributes** — _none_
- **in the lectures** — 0
- **classes seen** — _none in the committed lectures_

### `code` — `<code>`

- **attributes** — _none_
- **in the lectures** — 5
- **classes seen** — _none in the committed lectures_

```html
<code>lambda/a</code>
```

### `sup` — `<sup>`

- **attributes** — _none_
- **in the lectures** — 1
- **classes seen** — _none in the committed lectures_

```html
<sup>15</sup>
```

### `sub` — `<sub>`

- **attributes** — _none_
- **in the lectures** — 0
- **classes seen** — _none in the committed lectures_

### `u` — `<u>`

- **attributes** — _none_
- **in the lectures** — 0
- **classes seen** — _none in the committed lectures_

### `small` — `<small>`

- **attributes** — _none_
- **in the lectures** — 0
- **classes seen** — _none in the committed lectures_
